servers:
  - host: "192.168.25.212"
    port: 22
    username: "root"
    password: "YOUR_SERVER_PASSWORD"
    docker:
      container: "npu-train"
      workdir: "/workspace"
      create_cmd: "docker run -d --name npu-train --device=/dev/davinci0 --device=/dev/davinci_manager --device=/dev/devmm_svm --device=/dev/hisi_hdc -v /usr/local/dcmi:/usr/local/dcmi -v /usr/local/bin/npu-smi:/usr/local/bin/npu-smi -v /usr/local/Ascend/driver/lib64:/usr/local/Ascend/driver/lib64 -v /opt/npu-tools:/workspace -w /workspace ascend-train:latest sleep infinity"
  - host: "192.168.25.213"
    port: 22
    username: "root"
    password: "YOUR_SERVER_PASSWORD"
    docker:
      container: "npu-train"
      workdir: "/workspace"
      create_cmd: "docker run -d --name npu-train --device=/dev/davinci0 --device=/dev/davinci_manager --device=/dev/devmm_svm --device=/dev/hisi_hdc -v /usr/local/dcmi:/usr/local/dcmi -v /usr/local/bin/npu-smi:/usr/local/bin/npu-smi -v /usr/local/Ascend/driver/lib64:/usr/local/Ascend/driver/lib64 -v /opt/npu-tools:/workspace -w /workspace ascend-train:latest sleep infinity"
  - host: "192.168.25.214"
    port: 22
    username: "root"
    password: "YOUR_SERVER_PASSWORD"
    docker:
      container: "npu-train"
      workdir: "/workspace"
      create_cmd: "docker run -d --name npu-train --device=/dev/davinci0 --device=/dev/davinci_manager --device=/dev/devmm_svm --device=/dev/hisi_hdc -v /usr/local/dcmi:/usr/local/dcmi -v /usr/local/bin/npu-smi:/usr/local/bin/npu-smi -v /usr/local/Ascend/driver/lib64:/usr/local/Ascend/driver/lib64 -v /opt/npu-tools:/workspace -w /workspace ascend-train:latest sleep infinity"
  - host: "192.168.25.215"
    port: 22
    username: "root"
    password: "YOUR_SERVER_PASSWORD"
    docker:
      container: "npu-train"
      workdir: "/workspace"
      create_cmd: "docker run -d --name npu-train --device=/dev/davinci0 --device=/dev/davinci_manager --device=/dev/devmm_svm --device=/dev/hisi_hdc -v /usr/local/dcmi:/usr/local/dcmi -v /usr/local/bin/npu-smi:/usr/local/bin/npu-smi -v /usr/local/Ascend/driver/lib64:/usr/local/Ascend/driver/lib64 -v /opt/npu-tools:/workspace -w /workspace ascend-train:latest sleep infinity"
  - host: "192.168.25.216"
    port: 22
    username: "root"
    password: "YOUR_SERVER_PASSWORD"
  - host: "192.168.25.217"
    port: 22
    username: "root"
    password: "YOUR_SERVER_PASSWORD"
  - host: "192.168.25.218"
    port: 22
    username: "root"
    password: "YOUR_SERVER_PASSWORD"

feishu:
  app_id: "YOUR_FEISHU_APP_ID"
  app_secret: "YOUR_FEISHU_APP_SECRET"

script_deploy:
  remote_dir: "/opt/npu-tools"
  deploy_nodes:
    - "192.168.25.212"
    - "192.168.25.213"
    - "192.168.25.214"
    - "192.168.25.215"
  watch_dir: "."
